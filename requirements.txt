# Dependencies for the live backend: app.py + sports_agent/ (LangGraph agent)
fastapi==0.141.1
uvicorn==0.52.4
pydantic==2.13.4
python-dotenv==1.0.1
langchain-core==1.6.0
langchain-openai==1.6.0
langgraph==1.2.11
duckdb==1.4.4
pandas==3.0.5
numpy==2.5.2
scipy==1.18.1
statsmodels==0.14.6
requests==2.31.0
watchfiles  # lets uvicorn --reload actually watch for .py file changes on Windows
curl_cffi  # impersonates a real browser's TLS/HTTP fingerprint -- needed to get past
           # ESPN's site-api bot-mitigation, which a plain `requests` call gets 403'd by
           # even with browser-shaped headers. Unpinned deliberately (new enough to matter).
